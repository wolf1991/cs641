<IfModule mod_rewrite.c>
    RewriteEngine On

    # Directly visit if exits
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d

    # Redirect all visiting if not exist
    RewriteRule ^(.*)$ index.php?url=$1 [PT,L]
</IfModule>